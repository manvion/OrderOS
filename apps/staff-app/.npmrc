# Clerk's expo SDK pulls react-dom@19 as a peer (harmless for React Native), which trips
# npm's strict peer check against Expo 51's react@18. This is the standard Expo escape hatch
# and is required for `npm install` and EAS builds to succeed.
legacy-peer-deps=true
